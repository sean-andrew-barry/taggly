export * from "/js/String.js?after=/taggly/development/";
// import {String} from "/js/String.js?after=/taggly/development/";
import {Test} from "/js/Test.js";
import {Assert} from "/js/Assert.js";

// function UseASCII(expect)
// {
//   Assert.Class(Test)?.Has(Test, "Sync")?.Function(Test.Sync)
//   ?? Assert.Throw`Expected V${Test} to be a class that has a method called "Sync"`;

//   const value = "hello";
//   // const value = true;

//   Assert.String(value)?.Includes(value, "ello")
//   ?? Assert.Throw`Expected V${value} to be a string that includes the substring "ello", not T${value}`;

//   Assert.Undefined(value)
//   ?? Assert.String(value)
//   ?? Assert.Throw`Expected V${value} to be a string or undefined, not T${value}`;
// }

// Test.Sync(UseASCII);
